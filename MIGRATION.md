###SQL

USE `news`;
DROP TABLE `categorie`,`categories`, `news`, `news_read`;
USE `laravel_intranet`;
DROP TABLE `cache`, `cache_locks`, `failed_jobs`, `jobs`, `job_batches`, `migrations`, `modules`, `module_user`,
`password_reset_tokens`, `roles`, `role_user`, `sessions`, `users`;
USE `documents`;
DROP TABLE `categorie`,`categories`, `news`;
￼
